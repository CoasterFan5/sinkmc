{
  "name": "sinkmc",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build:web": "pnpm --filter sinkmc-web run build",
    "dev:web": "pnpm --filter sinkmc-web dev"
  }
}
